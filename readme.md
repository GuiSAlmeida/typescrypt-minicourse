Instalar typescript:
```sh
npm install -g typescript
```

Comando para compilar:
```sh
tsc index.ts --watch
```

Configurar tsconfig
```sh
tsc --init
```

```sh
tsc --watch
nodemon dist/classes.js
```
